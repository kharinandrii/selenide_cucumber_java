import java.util.*;

class MyNumber {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String myVar = scanner.nextLine();
        System.out.println(myVar.toLowerCase(Locale.ROOT));
        System.out.println(myVar.toUpperCase(Locale.ROOT));

    }
}